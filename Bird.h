#ifndef BIRD_H_INCLUDED
#define BIRD_H_INCLUDED
#include <iostream>
using namespace std;

class Bird::Animal.h{
private:
    float wingSpan;
public:
    Bird(float wingSpan);
    ~Bird();
    getwingSpan();
    setwingSpan();
};


#endif // BIRD_H_INCLUDED
